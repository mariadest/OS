The difference between starvation and a deadlock is that in a deadlock multiple processes are waiting for a condition to be satisfied, but the condition will never be satisfied. With starvation on the other hand multiple threads need to access a resource but they aren't permitted. 
